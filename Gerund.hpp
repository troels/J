#ifndef GERUND_HPP
#define GERUND_HPP

#include <algorithm>
#include <functional>
#include <numeric>

#include "JNoun.hpp"
#include "JVerbs.hpp"

namespace J {

bool is_gerund(JWord::Ptr word);


// class TieConjunction: public JConjunction {
//   JWord::Ptr operator()(JMachine::Ptr m, JWord::Ptr lword, JWord::Ptr rword) const { 
//     if (lword->get_grammar_class() == grammar_class_noun && rword->get_grammar_class == grammar_class_noun) {
//       return concat(m, lword, rword);
//     } else {
      

  
}
#endif
